<% if user_signed_in? %>
  <%= link_to "サインアウト", destroy_user_session_path, method: :delete, class: "home_text1" %>
<% end %> 

<% if !user_signed_in? %> 
  <%= link_to "サインイン", "/users/sign_in", class: "btn btn-primary" %>
<% end %> 

<% if user_signed_in? %>
  <p class="home_text2">右上の投稿をクリック！</p>
<% end %> 